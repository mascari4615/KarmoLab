# GitHub Copilot Instructions

## 1. 공통 사항
- **언어 및 어조**: 모든 답변과 주석은 **한국어**로 작성해주세요.
- 친절하고 명확한 어조를 사용해주세요.

---

## 2. YawnBot (Discord Bot)
**[적용 범위]** `YawnBot/` 디렉터리 내의 파일 작업 시 적용

이 프로젝트는 C# .NET 9.0 기반의 디스코드 봇 프로젝트입니다.

### 2.1. 아키텍처 및 디자인 패턴
- **서비스 지향 아키텍처(SOA)**를 따릅니다. 모든 비즈니스 로직은 `Services/` 폴더 내의 서비스 클래스에 구현하세요.
- **의존성 주입(DI)** 패턴을 사용하세요. `Program.cs`에서 서비스를 생성하고, 생성자를 통해 주입받으세요.
- 데이터 모델은 `Models/` 폴더에 정의하세요.
- 명령어 처리는 `CommandService`를 통해 등록하고 관리하세요.

### 2.2. 데이터 관리
- 게임 데이터는 `GameDataService`를 통해 관리합니다.
- 모든 데이터 파일(.json)은 `Data/` 폴더에 저장되어야 합니다.

### 2.3. 에러 처리 및 로깅
- 예외 처리는 철저히 해야 합니다.
- 사용자에게는 친절한 에러 메시지를 보여주고, 내부적인 에러는 `LoggingService`를 사용하여 `Data/error_logs.json`에 기록하세요.
- `try-catch` 블록을 적절히 사용하여 봇이 멈추지 않도록 하세요.

### 2.4. 코딩 스타일
- 들여쓰기는 **Tab**을 사용하세요.

### 2.5. 관리자 기능
- 관리자 전용 기능은 `ConfigService.IsAdmin(userId)`를 통해 권한을 확인해야 합니다.

---

## 3. Launcher (WPF)
**[적용 범위]** `Launcher/` 디렉터리 내의 파일 작업 시 적용

### 3.1. 빌드 및 실행 시 주의사항 (AI 필독)
- **재빌드/재실행 시 프로세스 충돌 방지**:
  - 이미 `Launcher.exe`가 실행 중이라면 파일 잠금으로 인해 빌드가 실패할 수 있습니다.
  - 반드시 먼저 프로세스를 강제 종료하세요:
    `Get-Process Launcher -ErrorAction SilentlyContinue | Stop-Process -Force`
  - **주의**: 위 종료 명령 실행 후 즉시 `dotnet run` 등을 이어서(chaining) 실행하지 말고, 확실히 종료된 후 실행 명령을 내리세요.
- **변경 이력 관리**:
  - `Doc/` 폴더 내의 History 문서에 작업 내역을 잘 기록해주세요.
